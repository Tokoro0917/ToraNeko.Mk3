# main-board

ToraNeko.Mk3 のメイン基板（1枚基板・全機能統合、基板一体型シャーシ）です。

KiCadプロジェクト本体（`.kicad_pro` / `.kicad_sch` / `.kicad_pcb`）はまだKiCad上で作成していません。
このディレクトリには、実際に回路図を起こす前に固めておく機能ブロック構成・ペリフェラル割り当て案・BOMドラフトを置いています。

## 機能ブロック

| ブロック | 内容 |
|---|---|
| MCU | STM32F411CEU6 |
| モータドライバ | MP6551 × 2（左モータ用・右モータ用） |
| モータ | NFP-D0812-1-3.7（ブラシ付きDC）× 2 |
| エンコーダ | AS5047P（磁気式）× 2、左右モータ軸に取り付け |
| IMU | LSM6DSRXTR |
| 壁センサ | 反射型IR。受光: LTR-209、発光: OSI5FU3A11C。前方斜め左右2組・側面左右2組の計4組 |
| 吸引ファン | サクションファン × 1（型番未定）。NチャネルMOSFETによるローサイドPWM駆動 |
| 電源 | 2セルLiPo入力 → MCU/センサ用3.3V系（**LXDC55FAAA-203**）、モータドライバ（MP6551）はLiPo直結 |
| デバッグ/書き込み | SWD、USB（STM32F411CEU6のUSB OTG FSを想定） |

## ペリフェラル割り当て案（STM32F411CEU6）

正確なピン番号はKiCadで回路図を起こす際にデータシート（UFQFPN48パッケージ）で確定してください。
ここではどのペリフェラルをどのブロックに割り当てるかの方針のみ示します。

- **SPIバス（IMU・エンコーダ用）**: LSM6DSRXTR、AS5047P × 2 は共通SPIバス上にCS（チップセレクト）を分けて接続する案。SPIバスを分割するかは配線の都合で判断。
- **PWM（モータ駆動用）**: MP6551 × 2 に対し、1モータあたり2本（IN1/IN2、もしくはPWM+DIR）を想定。TIM1などの高機能タイマーを優先的に割り当てる。
- **ADC（壁センサ用）**: LTR-209（受光）× 4組ぶんでADC 4チャンネルを使用。OSI5FU3A11C（発光）側は1組ずつ順番に点灯・測定するトランジスタ駆動用GPIOを4本確保し、隣接センサ間のクロストークを避ける。
- **PWM（吸引ファン用）**: MOSFETゲート駆動用に1チャンネル。モータ駆動用タイマーとは別チャンネルを割り当てる。
- **USB**: PA11/PA12（USB OTG FS）をデバッグ・書き込み・ログ出力用に確保する想定。
- **SWD**: ST-Link等での書き込み・デバッグ用に確保。

## 電源設計メモ

- 入力: 2セルLiPo（7.4V公称、満充電8.4V程度）
- MCU/センサ系: 3.3Vへ降圧、**LXDC55FAAA-203** を使用
- モータ系: LiPoをレギュレータを介さず**MP6551に直結**（MP6551の定格電圧範囲内であることを確認済みとして進める）
- 吸引ファン系: LiPoから供給、MOSFETでPWM制御（レギュレータを挟むかはファン選定後に確定）

## TODO

- [ ] SPIバスの分割方針（IMUとエンコーダを同一バスにするか）
- [ ] モータPWM用タイマー・チャンネルの確定
- [ ] 壁センサLED駆動用トランジスタ・抵抗値の選定
- [ ] 吸引ファンの型番選定
- [ ] 吸引ファン用MOSFET・ゲート抵抗・フライホイールダイオードの選定
- [ ] 吸引ファンの電源系統（LiPo直結か別レギュレータか）の判断
- [ ] 正式なピンアサイン表の作成（KiCad回路図と同期）
- [ ] KiCadプロジェクトの作成（`.kicad_pro` / `.kicad_sch` / `.kicad_pcb`）
